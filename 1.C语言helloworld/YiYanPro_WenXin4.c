// 当然，这是一个简单的C语言程序，可以输出"Hello, World!"字符串。请看以下代码：

#include <stdio.h>  
  
int main() {  
    printf("Hello, World!\n");  
    return 0;  
}

// 这个程序的工作方式很简单。#include <stdio.h>告诉编译器要包括标准输入输出头文件，这使得程序能够使用printf等函数。

// main函数是程序的入口点。在main函数内，我们调用了printf函数来打印"Hello, World!"，并在末尾加上换行符\n以开始新的一行。

// return 0;表示程序成功执行完毕并返回0。在大多数操作系统中，返回0表示程序已成功完成，而非零返回值则表示发生了错误。
