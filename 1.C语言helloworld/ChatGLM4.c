// 当然，下面是一个简单的C语言程序，用于输出字符串 “Hello, World!”：

#include <stdio.h>

int main() {
    // 输出 "Hello, World!"
    printf("Hello, World!\n");
    return 0;
}

// 要运行这个程序，你需要一个C语言编译器。你可以使用GCC、Clang或者其他支持C语言的编译器。保存这个代码到一个文件中，例如 hello_world.c，然后在命令行中运行以下命令来编译和运行它：

// gcc hello_world.c -o hello_world
// ./hello_world

// 如果你的系统是Windows并且使用的是MinGW或者Cygwin，命令可能略有不同。
