// 当然可以。下面是一个简单的C语言程序，它会输出字符串 “Hello, World!”：

#include <stdio.h>

int main() {
    printf("Hello, World!\n");
    return 0;
}

// 这段代码首先包含了标准输入输出库 stdio.h，这样我们就可以使用 printf 函数。main 函数是每个C程序的入口点，printf 函数则用于输出字符串到控制台。当运行这个程序时，它会在屏幕上显示 “Hello, World!”。
